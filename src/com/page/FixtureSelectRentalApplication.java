package com.page;

//Query Table
public class FixtureSelectRentalApplication {
	// private final List<List<List<String>>> rentApp;

	/*
	 * public FixtureSelectRentalApplication() { final RentalApplicationPage
	 * rentAppPage = new RentalApplicationPage();
	 *
	 * rentApp = new ArrayList<List<List<String>>>(); // first row final
	 * List<String> cols = (new FixtureColumn(rentAppPage.getClass()
	 * .getSimpleName())).getFixtureColumns(); rentApp.add(Arrays.asList(
	 * Arrays.asList(cols.get(0), "tuyenlq"), Arrays.asList(cols.get(1),
	 * "HAWAII"))); rentApp.add(Arrays.asList( Arrays.asList(cols.get(0),
	 * rentAppPage.getEmail()), Arrays.asList(cols.get(1),
	 * rentAppPage.getSate()))); }
	 *
	 * public List<List<List<String>>> query() { return rentApp; }
	 */
}